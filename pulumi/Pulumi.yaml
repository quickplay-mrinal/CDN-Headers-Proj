name: cloudfront-jwt-security
runtime: python
description: CloudFront Function + JWT Security Infrastructure - All resources in ap-south-2 (CloudFront is global)

template:
  config:
    aws:region:
      description: The AWS region to deploy infrastructure (default ap-south-2)
      default: ap-south-2
    aws_region:
      description: Custom region configuration for the application
      default: ap-south-2